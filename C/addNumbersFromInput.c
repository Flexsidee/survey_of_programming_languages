#include <stdio.h>

int main()
{
    int a, b, sum;
    
    prinf("Enter the first value: ");
    scanf("%d", a);
    
    printf("Enter the second value: ");
    scanf("%d", b);
    
    sum = a + b;
    printf("The sum is %i", sum);
    
    return (0);
}
